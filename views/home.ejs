<%- include("partails/header.ejs") %>
<main >
    <div >
        <div >
            <p><%= daynames[day] %>, <%=monthnames[month]%> <%= dayt %></p>
        </div>

        <form  action="/adder" method="POST">
            <input type="text" name="newItem2" placeholder=" New Item" autocomplete="off" required="" class="h-[42px] w-[320px] relative left-[10px] top-[9px] rounded-2xl border border-black">
            <button type="submit" name="submit-button" >+</button>
        </form>

            <div >
                <div >
                    <% for(var i = listItems2.length -1; i >= 0; i--){ %>
                        <div class="flex">
                            <input type="checkbox" id="todo" name="checker" class="h-7 w-7 m-3 flex-shrink-0">
                            <label for="todo" class="text-[32px] font-[200]"><%= listItems2[i] %></label>
                        </div>
                    <% } %>
                </div>
            </div>            
    </div>
    </main>
<%- include("partails/footer.ejs") %>